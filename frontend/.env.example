# Backend API origin used by frontend fetch calls.
# Default (when unset) is the production backend on Vercel.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Set to "false" to hide all passkey UI (login button, conditional autofill,
# signup enrollment prompt, profile management card). Defaults to enabled.
NEXT_PUBLIC_PASSKEYS_ENABLED=true
