{{- if .Values.mcp.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "estatewise.componentName" (dict "root" . "component" "mcp") }}
  namespace: {{ include "estatewise.namespace" . }}
  labels:
    {{- include "estatewise.componentLabels" (dict "root" . "component" "mcp") | nindent 4 }}
    {{- with .Values.global.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.mcp.replicaCount }}
  selector:
    matchLabels:
      {{- include "estatewise.selectorLabels" (dict "root" . "component" "mcp") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "estatewise.selectorLabels" (dict "root" . "component" "mcp") | nindent 8 }}
        {{- with .Values.mcp.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- if or .Values.mcp.podAnnotations .Values.global.annotations .Values.datadog.enabled }}
      annotations:
        {{- if .Values.global.annotations }}
        {{- toYaml .Values.global.annotations | nindent 8 }}
        {{- end }}
        {{- if .Values.mcp.podAnnotations }}
        {{- toYaml .Values.mcp.podAnnotations | nindent 8 }}
        {{- end }}
        {{- if .Values.datadog.enabled }}
        ad.datadoghq.com/mcp.logs: '[{"source":"nodejs","service":"estatewise-mcp"}]'
        ad.datadoghq.com/mcp.tags: '{"team":"estatewise","component":"mcp"}'
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "estatewise.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mcp.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: mcp
          image: {{ include "estatewise.image" (dict "Values" .Values "image" .Values.mcp.image) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.mcp.containerPort }}
          {{- if or .Values.mcp.env .Values.datadog.enabled }}
          env:
            {{- with .Values.mcp.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.datadog.enabled }}
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DD_TRACE_AGENT_PORT
              value: {{ .Values.datadog.agent.apm.port | quote }}
            - name: DD_SERVICE
              value: "estatewise-mcp"
            - name: DD_ENV
              value: "production"
            - name: DD_VERSION
              value: {{ .Values.mcp.image.tag | quote }}
            - name: DD_LOGS_INJECTION
              value: "true"
            {{- end }}
          {{- end }}
          {{- if and .Values.mcp.probes.readiness .Values.mcp.probes.readiness.enabled }}
          readinessProbe:
            {{- omit .Values.mcp.probes.readiness "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- if and .Values.mcp.probes.liveness .Values.mcp.probes.liveness.enabled }}
          livenessProbe:
            {{- omit .Values.mcp.probes.liveness "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- with .Values.mcp.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.mcp.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.mcp.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mcp.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mcp.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mcp.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
